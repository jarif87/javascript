// first.className = "text-white blue";
// first.classList.add("red");
// first.classList.remove("red");
// const first = document.getElementById("first"); // select the element

// console.log(first.classList.contains("red")); // prints true or false in browser console

first.classList.toggle("green"); // adds the class if not present, removes if present
